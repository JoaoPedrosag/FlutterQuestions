import 'package:flutter/material.dart';

class Resultado extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Você não conhece o Joãozinho, suma daqui!',
        style: TextStyle(fontSize: 28),
      ),
    );
  }
}
